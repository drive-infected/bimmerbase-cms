'use strict';

/**
 * special-version-category service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::special-version-category.special-version-category');
