'use strict';

/**
 * special-version-category router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::special-version-category.special-version-category');
