'use strict';

/**
 * option-availability controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::option-availability.option-availability');
